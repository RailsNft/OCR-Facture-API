# Descriptions courtes optimisées pour RapidAPI

## 🎯 Short Description (500 caractères max)

### Version recommandée :

```
Convert PDF invoices to JSON and Factur-X XML (EN16931 standard for France / Europe). Extract amounts, dates, invoice numbers, vendor, client, banking info, and structured tables automatically with confidence scores. Multi-language support. Batch processing. Webhooks for Zapier/Make/Salesforce.
```

**Longueur** : ~220 caractères ✅

---

### Version alternative (si besoin de plus court) :

```
Extract structured data from French invoices using OCR. Convert to JSON and Factur-X XML. Automatic extraction of amounts, dates, invoice numbers, vendor, client, banking info, and tables. Multi-language. Batch processing. Webhooks available.
```

**Longueur** : ~180 caractères ✅

---

### Version ultra-courte (si limité à 150 caractères) :

```
Extract invoice data to JSON/Factur-X XML. OCR for French invoices. Amounts, dates, numbers, vendor, client, banking info, tables. Multi-language. Batch processing.
```

**Longueur** : ~140 caractères ✅

---

## 📊 Mots-clés inclus (SEO)

✅ **ocr**  
✅ **invoice**  
✅ **facturx**  
✅ **pdf**  
✅ **json**  
✅ **france**  
✅ **documents**  
✅ **accounting**  
✅ **finance**  
✅ **extraction**  
✅ **automation**  
✅ **batch**  
✅ **webhook**  

---

## 💡 Pourquoi cette description fonctionne

1. **Commence par l'action** : "Convert PDF invoices"
2. **Mentionne le standard** : "EN16931" (important pour France)
3. **Liste les fonctionnalités** : mots-clés multiples
4. **Mentionne les intégrations** : Zapier/Make/Salesforce
5. **Court mais complet** : max d'infos en peu de mots

---

## 🔄 Variations selon le contexte

### Si vous voulez mettre l'accent sur la France :
```
OCR API for French invoices. Convert to JSON and Factur-X XML (EN16931). Extract amounts, dates, invoice numbers, vendor, client, banking info automatically. Multi-language support. Batch processing.
```

### Si vous voulez mettre l'accent sur l'automatisation :
```
Automate invoice processing with OCR. Extract structured data (amounts, dates, numbers, vendor, client, banking info) from PDF invoices. Convert to JSON and Factur-X XML. Batch processing. Webhooks for Zapier/Make/Salesforce.
```

### Si vous voulez mettre l'accent sur la conformité :
```
Factur-X compliant invoice OCR API (EN16931 standard). Extract invoice data to JSON and XML. Automatic extraction of amounts, dates, numbers, vendor, client, banking info. Multi-language. Batch processing.
```

---

**Utilisez la version recommandée pour le meilleur équilibre SEO/contenu !** 🚀

